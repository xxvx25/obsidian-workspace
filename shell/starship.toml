# External Brain 2 Starship preset
add_newline = false

format = """
$directory$git_branch$git_status$python$nodejs$env_var.EXTERNAL_PROFILE$time
$character """

[character]
success_symbol = "[❯](bold green) "
error_symbol = "[✗](bold red) "

[directory]
truncation_length = 5
truncate_to_repo = true
style = "bold cyan"

[git_branch]
symbol = " "
style = "bold purple"
format = "[$symbol$branch]($style) "

[git_status]
conflicted = "⚔️ "
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇡${ahead_count}⇣${behind_count}"
modified = "✚${count}"
staged = "●${count}"
format = "([$all_status$ahead_behind]($style) )"

[python]
symbol = "🐍 "
style = "yellow"
format = "via [$symbol$virtualenv($version )]($style)"

[nodejs]
symbol = "⬢ "
style = "bold green"
format = "via [$symbol($version )]($style)"

[env_var.EXTERNAL_PROFILE]
variable = "EXTERNAL_BRAIN_PROFILE"
style = "dimmed white"
format = "[$symbol($env_value) ]($style)"

[time]
disabled = false
style = "bold blue"
format = "[🕒 $time]($style) "
